/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GenerateSchoolYearPipe } from './generateSchoolYear.pipe';

describe('Pipe: GenerateSchoolYeare', () => {
  it('create an instance', () => {
    let pipe = new GenerateSchoolYearPipe();
    expect(pipe).toBeTruthy();
  });
});
